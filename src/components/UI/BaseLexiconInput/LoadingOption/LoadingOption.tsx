import React from 'react';

import classes from './LoadingOption.module.scss';

const LoadingOption = () => {
    return <div className={classes.noneOfOption}>Loading...</div>;
};

export default LoadingOption;
