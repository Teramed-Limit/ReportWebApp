import React from 'react';

import classes from './MenuPlacer.module.scss';

const MenuPlacer = () => {
    return (
        <div className={classes.Template}>
            <h1>MenuPlacer component</h1>
        </div>
    );
};

export default MenuPlacer;
